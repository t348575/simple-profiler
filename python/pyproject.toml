[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "simple-profiler"
version = "0.1.0"
description = "Simple Perfetto-compatible profiler using decorators and context managers"
requires-python = ">=3.8"
